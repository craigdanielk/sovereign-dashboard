"use client";

import ComplianceCard from "./ComplianceCard";

export default function ISOPanel() {
  return (
    <ComplianceCard
      title="ISO 42001 Controls"
      metric="IN PROGRESS"
      light="grey"
      detail="Mapping in progress — configure ISO 42001 control coverage to enable automated tracking"
    />
  );
}
